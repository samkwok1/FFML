hydra:
  run:
    dir: ${sim.sim_dir}/${sim.sim_algo}/${sim.sim_id}

sim:
  sim_dir: "outputs"
  sim_algo: "perceptron"
  sim_id: "0"

env:
  algorithm: perceptron # logistic_regression, decision_trees, gda, perceptron, or naive_bayes
  position: wr #wr, te, or rb